import '@testing-library/jest-dom';
import {Counter} from './App';
/**
 * Write test cases for the Counter component using React Testing Library and Jest. Ensure your tests cover the following:
 * 1. The component renders correctly with an initial count of 0.
 * 2. Clicking the "Increment" button increases the count by 1.
 * 3. Clicking the "Decrement" button decreases the count by 1.
 * 4. The component displays the updated count correctly after each button click.
 */

describe('Counter component tests', () => {
  
});
